[package]
name = "purgatory-simulation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Authoritative simulation core. Must not depend on windowing or GPU APIs."

[dependencies]
purgatory-common = { path = "../common" }

[lints]
workspace = true
